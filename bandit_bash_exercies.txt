# Para empezar el juego nos pide entrar en la siguiente url.bandit.labs.overthewire.org# Con el usuario "bandit0" por el puerto "2220".# Entonces escribimos el siguiente comando en nuestra terminal.ssh bandit0@bandit.labs.overthewire.org -p 2220# Escribimos la contraseña "bandit0".# Nos dará acceso al usuario "bandit0"# Escribimos el comando "ls" para listar los directorios que existen.# Aparecerá un archivo llamado "readme" lo abrimos con el siguiente comando.cat readme# Como es un archivo de texto nos mostrará lo que hay dentro del archivo# Será la contraseña del usuario "bandit1"boJ9jbbUNNfktd78OOpsqOltutMc3MY1# Escribimos "exit" para salir-------------------------------------------------------------------------------# Iniciamos sesión nuevamente con el usuario "bandit1"ssh bandit1@bandit.labs.overthewire.org -p 2220# Escribimos la contraseña que habíamos obtenido previamente# Y volvemos a listar con el comendo "ls"# Nos dará un fichero que se llama "-"# Este tipo de ficheros con símbolos como nombres dan problemas # El sistema los reconoce como instrucciones # Tenemos que utilizar otra forma para acceder al contenido# Con el comando "pwd" nos muestra la ruta absoluta donde estamos# Así que la escribiremos y agregaremos el nombre del fichero para que lo abracat /home/bandit1/-cat /home/bandit1/*cat $(pwd)/-cat $(pwd)/*# Estas serian diferentes formas de abrir el mismo fichero# Así obtendremos la clave del siguiente usuario "bandit2"CV1DtqXWVFXTvM2F0k09SHz0YwRINYA9# Escribimos "exit" para salir-------------------------------------------------------------------------------# Iniciamos sesión nuevamente con el usuario "bandit2"ssh bandit2@bandit.labs.overthewire.org -p 2220# Escribimos la contraseña que habíamos obtenido previamente# Y volvemos a listar con el comendo "ls"# Nos da un archivo con espacios en el nombre# Lo abrimos alguno de los siguientes comandos# cat spaces\ in\ the\ filename# cat "spaces in this filename"# cat spa*# cat $(pwd)/spac*# cat $(pwd)/spaces*# cat *name# cat *this*# cat /home/bandit2/sp*# cat *# Estas serian diferentes formas de abrir el mismo fichero# Así obtendremos la clave del siguiente usuario "bandit3"UmHadQclWmgdLOKQ3YNgjWxGoRMb5luK# Escribimos "exit" para salir-------------------------------------------------------------------------------# Iniciamos sesión nuevamente con el usuario "bandit3"ssh bandit3@bandit.labs.overthewire.org -p 2220# Escribimos la contraseña que habíamos obtenido previamente# Y volvemos a listar con el comendo "ls"# En este ejercicio nos indican que la contraseña del usuario bandit4 está en# en un archivo oculto dentro de la carpeta "inhere"# El nombre de los archivos ocultos comienzan con un punto "." # Para acceder al contenido del archivo oculto lo haremos de la siguiente forma# Entramos a "inhere" con "cd" y ahi listamos# ls -als# o podemos ahorrarnos el comando "cd" con:# find .# find . -type f# find . -type f -printf "%f\t%p\t%u\t%g\t%m\n" | column -t# para ver los archivos# Y para acceder al contenido escribimos alguno de los siguientes comandos# cat inhere/.hidden# cat $(pwd)/inhere/.hidden# cat $(pwd)/inhere/.hi*# cat $(pwd)/inhere/.*en# find . -type f | xargs cat# find . -name .hidden | xargs cat# find . -type f | xargs grep "FgAB"pIwrPrtPN36QITSp3EQaw936yaFoFgAB# Estas serian diferentes formas de abrir el mismo fichero# Así obtendremos la clave del siguiente usuario "bandit4"# Escribimos "exit" para salir-------------------------------------------------------------------------------